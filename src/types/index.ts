export type assets = {
    img: string,
    code: string,
    name: string,
    rate: string,
    change: string,
}

export type ownedAssets = {
    img: string,
    code: string,
    name: string,
    asset_value: string,
    fiat_value: string,
}
